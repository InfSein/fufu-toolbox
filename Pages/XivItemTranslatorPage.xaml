<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="fufu_toolbox.Pages.XivItemTranslatorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Loaded="Page_Loaded"
    mc:Ignorable="d">

    <Grid Padding="24" RowSpacing="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock FontSize="30" FontWeight="SemiBold" Text="FFXIV 物品翻译" />

        <InfoBar
            x:Name="ToastInfoBar"
            Grid.Row="1"
            IsOpen="False"
            IsClosable="False"
            IsIconVisible="True" />

        <Border
            Grid.Row="2"
            Padding="12"
            CornerRadius="10"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1">
            <Grid ColumnSpacing="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock VerticalAlignment="Center" Text="忽略开头文本" />
                <TextBox
                    x:Name="IgnoredPrefixTextBox"
                    Width="200"
                    Grid.Column="1" />
                <CheckBox
                    x:Name="KeepPrefixCheckBox"
                    Grid.Column="2"
                    VerticalAlignment="Center"
                    Content="保留开头" />
            </Grid>
        </Border>

        <Grid Grid.Row="3" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border
                Grid.Column="0"
                Padding="12"
                CornerRadius="10"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1">
                <Grid RowSpacing="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid ColumnSpacing="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock FontSize="16" FontWeight="SemiBold" Text="输入" />
                        <Button Grid.Column="1" Content="清空" Click="ClearInputButton_Click" />
                    </Grid>
                    <Grid Grid.Row="1" ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock VerticalAlignment="Center" Text="输入语言" />
                        <ComboBox
                            x:Name="InputLanguageComboBox"
                            Grid.Column="1" />
                    </Grid>
                    <TextBox
                        x:Name="InputTextBox"
                        Grid.Row="2"
                        AcceptsReturn="True"
                        TextWrapping="Wrap"
                        ScrollViewer.VerticalScrollBarVisibility="Auto"
                        TextChanged="InputTextBox_TextChanged" />
                </Grid>
            </Border>

            <Border
                Grid.Column="1"
                Padding="12"
                CornerRadius="10"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1">
                <Grid RowSpacing="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid ColumnSpacing="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock FontSize="16" FontWeight="SemiBold" Text="输出" />
                        <Button Grid.Column="1" Content="复制" Click="CopyOutputButton_Click" />
                    </Grid>
                    <Grid Grid.Row="1" ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock VerticalAlignment="Center" Text="输出语言" />
                        <ComboBox
                            x:Name="OutputLanguageComboBox"
                            Grid.Column="1" />
                    </Grid>
                    <TextBox
                        x:Name="OutputTextBox"
                        Grid.Row="2"
                        IsReadOnly="True"
                        AcceptsReturn="True"
                        TextWrapping="Wrap"
                        ScrollViewer.VerticalScrollBarVisibility="Auto" />
                </Grid>
            </Border>
        </Grid>

        <Grid Grid.Row="4" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="ProgressTextBlock" Grid.Column="0" VerticalAlignment="Center" Text="0 / 0" />
            <ProgressBar
                x:Name="TranslateProgressBar"
                Grid.Column="1"
                VerticalAlignment="Center"
                Minimum="0"
                Maximum="1"
                Value="0" />
            <Button
                x:Name="TranslateButton"
                Grid.Column="3"
                HorizontalAlignment="Right"
                Content="开始翻译"
                IsEnabled="False"
                Click="TranslateButton_Click" />
        </Grid>
    </Grid>
</Page>
