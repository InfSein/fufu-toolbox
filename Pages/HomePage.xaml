<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="fufu_toolbox.Pages.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:fufu_toolbox.Models"
    mc:Ignorable="d">

    <Grid Padding="24">
        <ScrollViewer>
            <StackPanel Spacing="20">
                <Border Padding="20" CornerRadius="12" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                    <StackPanel Spacing="8">
                        <TextBlock FontSize="28" FontWeight="SemiBold" Text="浮浮工具箱" />
                        <TextBlock Opacity="0.85" TextWrapping="Wrap" Text="这里是工具总入口。后续每个小工具都可以作为独立模块接入，不影响已有功能。" />
                    </StackPanel>
                </Border>

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock FontSize="20" FontWeight="SemiBold" Text="工具预留区" />

                    <GridView
                        Grid.Row="1"
                        Margin="0,12,0,0"
                        IsItemClickEnabled="False"
                        ItemsSource="{x:Bind ViewModel.PlannedTools, Mode=OneWay}"
                        SelectionMode="None">
                        <GridView.ItemsPanel>
                            <ItemsPanelTemplate>
                                <ItemsWrapGrid MaximumRowsOrColumns="3" Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </GridView.ItemsPanel>
                        <GridView.ItemTemplate>
                            <DataTemplate x:DataType="models:ToolCardItem">
                                <Border
                                    Width="280"
                                    Height="150"
                                    Padding="14"
                                    Margin="0,0,12,12"
                                    Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                    BorderThickness="1"
                                    CornerRadius="10">
                                    <StackPanel Spacing="6">
                                        <TextBlock FontSize="18" FontWeight="SemiBold" Text="{x:Bind Name}" />
                                        <TextBlock Opacity="0.8" TextWrapping="WrapWholeWords" Text="{x:Bind Description}" />
                                        <StackPanel Orientation="Horizontal" Spacing="6">
                                            <TextBlock Text="分类:" />
                                            <TextBlock Text="{x:Bind Category}" />
                                            <TextBlock Margin="10,0,0,0" Text="状态:" />
                                            <TextBlock Text="{x:Bind Status}" />
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </GridView.ItemTemplate>
                    </GridView>
                </Grid>

                <Border
                    Padding="16"
                    BorderBrush="{ThemeResource AccentFillColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="10">
                    <TextBlock TextWrapping="Wrap" Text="新增工具时，建议以“一个工具一个页面 + 一个 ViewModel”方式接入，并在左侧导航或此处卡片中加入入口。" />
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
